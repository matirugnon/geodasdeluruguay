# Frontend public envs only
# Nunca agregar secretos privados del backend en este archivo.

# API pública consumida por el storefront.
# En desarrollo local puede omitirse y usar el proxy de Vite.
VITE_API_URL=/api
